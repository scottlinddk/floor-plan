# apps/bff

Intentionally empty beyond a workspace stub. Per the phased roadmap, the BFF
(routes, provider adapters, storage) is Phase 3 work — it exists once there's
a real digitization provider to wrap and a real client for
`/digitize`/`/projects` to serve. Building it earlier would mean guessing at
a provider's response shape.
